{
  "name": "admin-my-store-functions",
  "version": "1.0.0",
  "engines": { "node": "20" },
  "main": "index.js",
  "scripts": {
    "build": "echo No build step",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.1.1"
  }
}
